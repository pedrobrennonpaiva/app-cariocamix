import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    buttons: {
        flexDirection: "row",
        width: '100%'
    },
    btnHorizontalNavigator: {
        marginVertical: 10,
        width: 130,
        height: 45,
        borderRadius: 20,
        marginRight: 5
    },
});

